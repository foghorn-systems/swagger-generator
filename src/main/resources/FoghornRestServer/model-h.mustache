#pragma once

#include <map>
#include <vector>
#include <iostream>

#include <foghorn/wire/json.h>

namespace foghorn {
namespace em {

using Json = wire::json_t;

{{#models}}
{{#model}}
class {{classFilename}} {
  public:
     {{classFilename}}();
    ~{{classFilename}}();

     Json toJson(bool expand);
     void fromJson(const Json& j);

    {{#vars}}
        void {{setter}}(const {{{datatype}}}& {{name}}_) { {{name}} = {{name}}_; }
        {{{datatype}}} {{getter}}() { return {{name}}; }
    {{/vars}}

  private:
    {{#vars}}
        {{{datatype}}} {{name}};
    {{/vars}}
};
    }
    }
{{/model}}
{{/models}}

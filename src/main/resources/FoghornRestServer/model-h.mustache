#pragma once

#include <map>
#include <vector>
#include <iostream>
#include <foghorn/wire/json.h>
{{#models}}{{#model}}
#include <foghorn/em/api/rest/generated/model/{{classFilename}}.h>
{{/model}}{{/models}}

namespace foghorn {
namespace em {

using Json = wire::json_t;

{{#models}}
{{#model}}
class {{classFilename}} {
  public:
    {{classFilename}}();
    ~{{classFilename}}();

    Json toJson(bool expand);
    void fromJson(const Json& j);

    {{#vars}}
    void {{setter}}(const {{{datatype}}}& {{name}}_);
    {{{datatype}}} {{getter}}();
    {{/vars}}

  private:
    {{#vars}}
    {{{datatype}}} {{name}};
    {{/vars}}
};
} 
}
{{/model}}
{{/models}}
